# README Delta Report

Canonical target: `/Users/iv/Developer/Wildberries/cocoaskills/.temp/aid/docs/skill-readme-style.md`
Template: `/Users/iv/Developer/Wildberries/cocoaskills/.temp/aid/templates/skill-readme.md`

Required section order from the guide:

1. `# Skill Name` + короткий лид
2. `## Что делает скилл`
3. `## Основные сценарии`
4. `## Источники данных` (опционально, по применимости)
5. `## Команды`
6. `## Установка и интеграция`
7. `## Системные зависимости`
8. `## Ограничения и безопасность`

Hard rules (from project + guide):
- Русский, официально без канцелярита, короткие предложения, технические факты.
- Запрещено: жирный `**`, длинное тире `—`, кавычки-ёлочки `« »`, маркетинг, тяжёлый English-keyword mixing.
- Install = только CocoaSkills: `Skillfile.json` + `csk install` (проект) и `csk global add ... --git ... --tag vX.Y.Z` + `csk global install` (глобально). Без `make install`, без Windows `py -3 setup_main.py`, без legacy.
- Никаких секций `Структура репозитория` и `Сопровождение`/`Обновление и сопровождение`.

Note: reference READMEs `skill-gitlab` и `skill-sentry` сами ещё НЕ приведены к гайду (в них остался `make install`, жирный текст, секции `Структура`). Они не эталон для install-блока. Эталон install — это `skill-readme.md` template + раздел "Установка в README" гайда.

Git URL для install-команд:
- bi: `git@gitlab.wildberries.ru:portals/partner-mobile/agentic-infra/skills/skill-bi.git`, последний тег `v1.0.0`
- youtrack: `git@gitlab.wildberries.ru:portals/partner-mobile/agentic-infra/skills/skill-youtrack.git`, последний тег `v1.1.0`

---

## skill-bi README

Файл: `/Users/iv/agents/skills/skill-bi/README.md`

Состояние: это нетронутый дефолтный GitLab-шаблон ("Getting started / Add your files / Editing this README"). Английский, нулевое отношение к скиллу. README надо переписать с нуля по template.

### Present
- `# Skill BI` — единственное, что условно подходит как заголовок (но без лида).
- Всё остальное содержимое (`Getting started`, `Add your files`, `Integrate with your tools`, `Collaborate with your team`, `Test and Deploy`, `Editing this README`, `Suggestions for a good README`, ...) — мусор из GitLab-шаблона.

### Missing (все требуемые секции отсутствуют)
- Короткий лид под заголовком — нет.
- `## Что делает скилл` — нет.
- `## Основные сценарии` — нет.
- `## Источники данных` — нет (для BI-скилла, скорее всего, релевантна: источники BI/дашбордов/датасетов).
- `## Команды` — нет.
- `## Установка и интеграция` — нет (есть только git push инструкция из шаблона).
- `## Системные зависимости` — нет.
- `## Ограничения и безопасность` — нет.

### Violations
- Язык английский — нарушение (docs должны быть русскими).
- Маркетинговый/туториальный тон GitLab-шаблона.
- Markdown-чеклисты `- [ ]` со ссылками на docs.gitlab.com — мусор.
- Горизонтальный разделитель `***` (строка 45) — не предусмотрен.
- Install-блок неверный: показывает `git remote add origin ... && git push`, а не `csk install` / `csk global`. Полностью переписать.
- (Жирный/ёлочки/тире в этом файле почти не встречаются, потому что это просто чужой шаблон — но это не плюс, контент всё равно под снос.)

### Edits (переписать файл целиком по template)
1. Заголовок: `# Skill BI` (оставить) + добавить лид на 1-3 предложения: что делает BI-скилл, для каких агентских сред, какие сценарии (русский, без жирного/тире/ёлочек).
2. Удалить ВСЁ текущее содержимое со строки 3 по 94 (весь GitLab boilerplate, включая `***` и `# Editing this README`).
3. Добавить `## Что делает скилл` — bullet-список основных сценариев (нужно вытащить реальные сценарии из `SKILL.md` скилла; в README сейчас данных нет).
4. Добавить `## Основные сценарии` — таблица `| Сценарий | Описание |`.
5. Добавить `## Источники данных` — таблица источников BI (датасеты/дашборды/инстансы), если применимо к скиллу.
6. Добавить `## Команды` — таблица `| Команда | Назначение |` с CLI-командами скилла (взять из `SKILL.md`/`scripts/`).
7. Добавить `## Установка и интеграция` строго по шаблону:
   ```json
   {
     "name": "skill-bi",
     "git": "git@gitlab.wildberries.ru:portals/partner-mobile/agentic-infra/skills/skill-bi.git",
     "tag": "v1.0.0"
   }
   ```
   ```bash
   csk install
   ```
   ```bash
   csk global add skill-bi --git git@gitlab.wildberries.ru:portals/partner-mobile/agentic-infra/skills/skill-bi.git --tag v1.0.0
   csk global install
   ```
8. Добавить `## Системные зависимости` — таблица `| Зависимость | Для чего нужна |` (если есть внешние CLI/тулзы; если нет — секцию можно опустить).
9. Добавить `## Ограничения и безопасность` — read-only по умолчанию, мутации по явному запросу, зависимости не ставятся автоматически.
10. Контент сценариев/команд НЕЛЬЗЯ выдумать — нужно прочитать `SKILL.md` и `scripts/` skill-bi, чтобы наполнить секции реальными данными. README сейчас не содержит ни одной фактической детали о скилле.

---

## skill-youtrack README

Файл: `/Users/iv/agents/skills/skill-youtrack/README.md`

Состояние: подробный, технически полный, по-русски, но построен по СТАРОЙ структуре (длинный нарратив + `Структура репозитория` + `Архитектура` + `Обновление и сопровождение`) и нарушает install-правила (`make install`, Windows `py -3`). Требует существенной реструктуризации под template, плюс чистка стиля.

### Present (есть, но не в порядке/форме гайда)
- `# skill-youtrack` + развёрнутый лид (строки 7-28) — лид есть, но слишком длинный, многословный.
- `## Что делает навык` (строка 30) — есть, по сути соответствует `## Что делает скилл` (терминология: гайд/template говорит "скилл", здесь "навык" — расхождение, но не жёсткий запрет; стоит унифицировать).
- Команды по факту описаны (секции `Использование CLI`, `Модель инстансов`, `Использование в агентной среде`), но НЕ в виде таблицы `## Команды`.
- Install присутствует (`## Установка`), но неверный по форме.
- Требования присутствуют (`## Требования`) — близко к `## Системные зависимости`, но прозой, не таблицей.

### Missing (требуемые секции по гайду)
- `## Основные сценарии` (как таблица) — нет.
- `## Источники данных` — нет (для youtrack малорелевантно, можно опустить).
- `## Команды` как отдельная таблица — нет (команды размазаны по многим прозаическим секциям).
- `## Установка и интеграция` в формате CocoaSkills — отсутствует (есть `make`-вариант, см. violations).
- `## Системные зависимости` как таблица — нет (есть прозаический `## Требования`).
- `## Ограничения и безопасность` — явной секции нет (отдельные предупреждения про preview-first/scoped разбросаны по тексту).

### Violations
Жирный `**` — отсутствует (хорошо).
Ёлочки `« »` — отсутствуют (хорошо).
Длинное тире `—`: ПРИСУТСТВУЕТ, надо убрать. Конкретные места:
- стр. 294: `Имя инстанса — это локальный идентификатор...`
- стр. 327-328: `основная проблема — начинать...`
- стр. 375: `--mine определяет...` (в этой строке тире нет, но проверить соседние)
- стр. 382-383: `Это рассчитано на доски... На крупных инстансах...` (есть `—`)
- стр. 480: `--locale можно не указывать —`
- (прогнать весь файл grep'ом на `—` перед правкой; их несколько.)

Install-правила нарушены грубо:
- стр. 163-164: `make install REPO=... LOCALE=ru` — запрещено.
- стр. 169-170: Windows `py -3 scripts/setup_main.py ...` — запрещено.
- стр. 193-194: `make -C <repo>/... skill` — запрещено.
- стр. 200-207: Windows `py -3 ... bootstrap_runtime.py` / `.cmd` — запрещено.
- стр. 474, 487, 499: `make install` / `make -C ... skill` / `bootstrap_runtime.py --force` в секции сопровождения — запрещено.
Нет НИ ОДНОЙ команды `csk install` / `csk global add` / `csk global install`. Install-блок полностью отсутствует в нужной форме.

Запрещённые секции присутствуют:
- `## Структура репозитория` (стр. 70-111) — удалить целиком.
- `## Архитектура` (стр. 113-153) — не предусмотрена гайдом, под снос (или резко ужать; гайд её не содержит).
- `## Обновление и сопровождение` (стр. 462-510) — удалить целиком (секция "Сопровождение" запрещена).
- `Makefile`-упоминания в `Структура` и `Архитектура` — уходят вместе с секциями.

Тон/многословность:
- Лид (7-28) — слишком длинный для "короткого лида", сильный English-keyword mixing (`multi-instance auth`, `runtime`, `scoped board ids`, `board-oriented`, `preview-first`). Гайд требует естественный русский без тяжёлого микса. Поджать и русифицировать где возможно.
- Множество англицизмов разбросано по тексту (`board surface`, `unrestricted board discovery`, `agent-friendly`, `in-process`, `pinning`). Допустимы технические термины, но плотность высокая — снизить.

Прочее:
- `## Лицензия` (стр. 512-522) — гайд её не перечисляет среди требуемых; не запрещена явно, но в template её нет. Можно оставить кратко или убрать в `<details>`. Длинные платформенные/первый-логин инструкции по auth гайд предписывает прятать в `<details>`.

### Edits
1. Сжать лид (стр. 7-28) до 2-4 предложений: что делает скилл, для каких сред, ключевые сценарии. Русифицировать, убрать тяжёлый англо-микс.
2. Секцию `## Что делает навык` оставить, но переименовать в `## Что делает скилл` (унификация с гайдом/template) и почистить bullet'ы от лишних англицизмов.
3. Добавить `## Основные сценарии` — таблица `| Сценарий | Описание |` (board current/my-tasks/tasks, issue brief/show/search, create-task/subtask, comment-add, membership).
4. Добавить `## Команды` — таблица `| Команда | Назначение |` для `yt` и `ytx` (сейчас это прозаические блоки `Использование CLI`, `Модель инстансов`). Длинные списки команд по платформам и первый логин — спрятать в `<details>`.
5. Удалить `## Структура репозитория` (70-111) целиком.
6. Удалить `## Архитектура` (113-153) целиком (или, если очень нужно, ужать в один абзац без отдельной секции; гайд её не предусматривает).
7. Заменить весь `## Установка` (155-234) на `## Установка и интеграция` по template:
   ```json
   {
     "name": "skill-youtrack",
     "git": "git@gitlab.wildberries.ru:portals/partner-mobile/agentic-infra/skills/skill-youtrack.git",
     "tag": "v1.1.0"
   }
   ```
   ```bash
   csk install
   ```
   ```bash
   csk global add skill-youtrack --git git@gitlab.wildberries.ru:portals/partner-mobile/agentic-infra/skills/skill-youtrack.git --tag v1.1.0
   csk global install
   ```
   Убрать все `make`/`py -3 setup_main.py`/`bootstrap_runtime` команды. Правила выбора локали (стр. 229-234) — либо убрать, либо в `<details>` (это installer-specific, к CocoaSkills не относится).
8. Преобразовать `## Требования` (47-68) в `## Системные зависимости` — таблица `| Зависимость | Для чего нужна |` (Python 3.10-3.13, git для `--mine`, сетевой доступ для bootstrap, доступ к YouTrack). Подробности про локали/bootstrap — в `<details>` или убрать.
9. Первый логин / auth (235-290) — спрятать в `<details>` (гайд требует прятать длинные auth/platform-specific инструкции).
10. Добавить `## Ограничения и безопасность`: preview-first для мутаций (без `--apply` ничего не применяется), scoped boards на крупных инстансах, секреты только в системном keyring, зависимости не ставятся автоматически.
11. Удалить `## Обновление и сопровождение` (462-510) целиком.
12. Убрать ВСЕ длинные тире `—` по файлу (заменить на двоеточие/перестроить предложение). Прогнать `grep -n '—'` перед финальной вычиткой.
13. `## Лицензия` (512-522) — оставить кратко или перенести в `<details>`; не делать отдельной крупной секцией (в template её нет).
14. Снизить плотность англицизмов в остаточном тексте, сохранив только устоявшиеся технические термины.
