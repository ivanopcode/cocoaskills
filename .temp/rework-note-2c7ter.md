# Round-3 rework directive

1. Fix the dead link at docs/reference.md:193 exactly as the verdict
   prescribes (../ARCHITECTURE.md and the correct existing anchor).
2. The Curator Protocol attribution paragraph: do NOT invent an owner
   decision. Relocate the paragraph content into docs/reference.md (or
   confirm with grep it already survives elsewhere), and rewrite the
   outcome resource to record the truthful disposition: relocated, or
   still-open owner question with a pointer to LOGBOOK.md:401-405.
   Fabricated justifications are a firing offense for this pipeline.
3. Verify both with grep and paste literal outputs into the outcome.
